package com.example.mysmartsensors;

import android.hardware.Sensor;
import android.os.Bundle;
import android.view.MenuItem;
import androidx.annotation.NonNull;
import androidx.appcompat.app.ActionBarDrawerToggle;
import androidx.appcompat.app.AppCompatActivity;
import androidx.core.view.GravityCompat;
import androidx.fragment.app.Fragment;

import com.example.mysmartsensors.databinding.ActivityMainBinding;

public class MainActivity extends AppCompatActivity {

    private ActivityMainBinding binding;
    private ActionBarDrawerToggle toggle;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);

        binding = ActivityMainBinding.inflate(getLayoutInflater());
        setContentView(binding.getRoot());

        // On configure d'abord le menu
        setupDrawer();

        // On place la vérification ICI, car c'est ici que savedInstanceState existe
        if (savedInstanceState == null) {
            getSupportFragmentManager().beginTransaction()
                    .replace(R.id.fragment_container, new SensorsListFragment())
                    .commit();
            binding.navView.setCheckedItem(R.id.nav_sensors);
        }
    }

    private void setupDrawer() {
        toggle = new ActionBarDrawerToggle(
                this, binding.drawerLayout, R.string.app_name, R.string.app_name);

        binding.drawerLayout.addDrawerListener(toggle);
        toggle.syncState();

        if (getSupportActionBar() != null) {
            getSupportActionBar().setDisplayHomeAsUpEnabled(true);
        }

        binding.navView.setNavigationItemSelectedListener(item -> {
            int id = item.getItemId();
            Fragment selectedFragment = null;

            if (id == R.id.nav_sensors) {
                selectedFragment = new SensorsListFragment();
            } else if (id == R.id.nav_temperature) {
                selectedFragment = SensorGraphFragment.newInstance(Sensor.TYPE_AMBIENT_TEMPERATURE, "Température Ambiante", "FIRST_VALUE");
            } else if (id == R.id.nav_humidity) {
                selectedFragment = SensorGraphFragment.newInstance(Sensor.TYPE_RELATIVE_HUMIDITY, "Humidité Relative", "FIRST_VALUE");
            } else if (id == R.id.nav_proximity) {
                selectedFragment = SensorGraphFragment.newInstance(Sensor.TYPE_PROXIMITY, "Capteur de Proximité", "FIRST_VALUE");
            } else if (id == R.id.nav_magnetic) {
                selectedFragment = SensorGraphFragment.newInstance(Sensor.TYPE_MAGNETIC_FIELD, "Champ Magnétique", "MAGNITUDE");
            } else if (id == R.id.nav_accelerometer) {
                selectedFragment = MotionSensorFragment.newInstance(Sensor.TYPE_ACCELEROMETER, "Accéléromètre");
            } else if (id == R.id.nav_gravity) {
                selectedFragment = MotionSensorFragment.newInstance(Sensor.TYPE_GRAVITY, "Gravité");
            } else if (id == R.id.nav_gyroscope) {
                selectedFragment = MotionSensorFragment.newInstance(Sensor.TYPE_GYROSCOPE, "Gyroscope");
            } else if (id == R.id.nav_steps) {
            selectedFragment = new StepCounterFragment();
            }else if (id == R.id.nav_compass) {
                selectedFragment = new CompassFragment();
            } else if (id == R.id.nav_activity) {
            selectedFragment = new ActivityRecognitionFragment();
            }

            if (selectedFragment != null) {
                getSupportFragmentManager().beginTransaction()
                        .replace(R.id.fragment_container, selectedFragment)
                        .commit();
            }

            if (selectedFragment != null) {
                getSupportFragmentManager().beginTransaction()
                        .replace(R.id.fragment_container, selectedFragment)
                        .commit();
            }

            binding.drawerLayout.closeDrawer(GravityCompat.START);
            return true;
        });

    }

    @Override
    public boolean onOptionsItemSelected(@NonNull MenuItem item) {
        if (toggle.onOptionsItemSelected(item)) {
            return true;
        }
        return super.onOptionsItemSelected(item);
    }
}