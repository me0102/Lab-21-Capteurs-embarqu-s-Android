package com.example.mysmartsensors;

import android.content.Context;
import android.hardware.Sensor;
import android.hardware.SensorEvent;
import android.hardware.SensorEventListener;
import android.hardware.SensorManager;
import android.os.Bundle;
import android.os.Handler;
import android.os.Looper;
import android.view.LayoutInflater;
import android.view.View;
import android.view.ViewGroup;
import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import androidx.fragment.app.Fragment;

import com.example.mysmartsensors.databinding.FragmentCompassBinding;

public class CompassFragment extends Fragment implements SensorEventListener {

    private FragmentCompassBinding binding;
    private SensorManager sensorManager;
    private Sensor accelerometer;
    private Sensor magnetometer;

    private final float[] gravityValues = new float[3];
    private final float[] magneticValues = new float[3];
    private boolean hasGravity = false;
    private boolean hasMagnetic = false;

    // Simulation
    private final Handler simulationHandler = new Handler(Looper.getMainLooper());
    private float simulatedDegree = 0f;

    @Nullable
    @Override
    public View onCreateView(@NonNull LayoutInflater inflater, @Nullable ViewGroup container, @Nullable Bundle savedInstanceState) {
        binding = FragmentCompassBinding.inflate(inflater, container, false);

        sensorManager = (SensorManager) requireActivity().getSystemService(Context.SENSOR_SERVICE);
        if (sensorManager != null) {
            accelerometer = sensorManager.getDefaultSensor(Sensor.TYPE_ACCELEROMETER);
            magnetometer = sensorManager.getDefaultSensor(Sensor.TYPE_MAGNETIC_FIELD);
        }

        return binding.getRoot();
    }

    @Override
    public void onResume() {
        super.onResume();
        if (accelerometer != null && magnetometer != null) {
            sensorManager.registerListener(this, accelerometer, SensorManager.SENSOR_DELAY_UI);
            sensorManager.registerListener(this, magnetometer, SensorManager.SENSOR_DELAY_UI);
            binding.textCompassStatus.setVisibility(View.GONE);
        } else {
            binding.textCompassStatus.setText("Capteurs manquants. Simulation activée.");
            binding.textCompassStatus.setVisibility(View.VISIBLE);
            startSimulation();
        }
    }

    @Override
    public void onPause() {
        super.onPause();
        sensorManager.unregisterListener(this);
        simulationHandler.removeCallbacksAndMessages(null);
    }

    @Override
    public void onSensorChanged(SensorEvent event) {
        // 1. On récupère les valeurs des deux capteurs
        if (event.sensor.getType() == Sensor.TYPE_ACCELEROMETER) {
            System.arraycopy(event.values, 0, gravityValues, 0, 3);
            hasGravity = true;
        } else if (event.sensor.getType() == Sensor.TYPE_MAGNETIC_FIELD) {
            System.arraycopy(event.values, 0, magneticValues, 0, 3);
            hasMagnetic = true;
        }

        // 2. Si on a les deux, on calcule la matrice de rotation
        if (hasGravity && hasMagnetic) {
            float[] rotationMatrix = new float[9];
            float[] orientation = new float[3];

            boolean success = SensorManager.getRotationMatrix(rotationMatrix, null, gravityValues, magneticValues);

            if (success) {
                SensorManager.getOrientation(rotationMatrix, orientation);

                // orientation[0] est l'azimut en radians. On le convertit en degrés.
                float azimuthDegrees = (float) Math.toDegrees(orientation[0]);

                // Pour éviter les valeurs négatives (ex: -90° devient 270°)
                if (azimuthDegrees < 0) {
                    azimuthDegrees += 360;
                }

                updateCompassUI(azimuthDegrees);
            }
        }
    }

    private void updateCompassUI(float degree) {
        binding.textDegree.setText(Math.round(degree) + "°");
        binding.textDirection.setText(getDirectionName(degree));
    }

    // Transforme l'angle en point cardinal
    private String getDirectionName(float degree) {
        if (degree >= 337.5 || degree < 22.5) return "Nord";
        if (degree >= 22.5 && degree < 67.5) return "Nord-Est";
        if (degree >= 67.5 && degree < 112.5) return "Est";
        if (degree >= 112.5 && degree < 157.5) return "Sud-Est";
        if (degree >= 157.5 && degree < 202.5) return "Sud";
        if (degree >= 202.5 && degree < 247.5) return "Sud-Ouest";
        if (degree >= 247.5 && degree < 292.5) return "Ouest";
        return "Nord-Ouest";
    }

    @Override
    public void onAccuracyChanged(Sensor sensor, int accuracy) {}

    private void startSimulation() {
        simulationHandler.postDelayed(new Runnable() {
            @Override
            public void run() {
                // Fait tourner la boussole lentement pour la simulation
                simulatedDegree = (simulatedDegree + 2f) % 360f;
                updateCompassUI(simulatedDegree);
                simulationHandler.postDelayed(this, 100);
            }
        }, 100);
    }

    @Override
    public void onDestroyView() {
        super.onDestroyView();
        binding = null;
    }
}