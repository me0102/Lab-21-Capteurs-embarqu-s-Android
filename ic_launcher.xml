package com.example.mysmartsensors;

import android.Manifest;
import android.content.Context;
import android.content.pm.PackageManager;
import android.hardware.Sensor;
import android.hardware.SensorEvent;
import android.hardware.SensorEventListener;
import android.hardware.SensorManager;
import android.os.Build;
import android.os.Bundle;
import android.os.Handler;
import android.os.Looper;
import android.view.LayoutInflater;
import android.view.View;
import android.view.ViewGroup;
import androidx.activity.result.ActivityResultLauncher;
import androidx.activity.result.contract.ActivityResultContracts;
import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import androidx.core.content.ContextCompat;
import androidx.fragment.app.Fragment;

import com.example.mysmartsensors.databinding.FragmentStepCounterBinding;

public class StepCounterFragment extends Fragment implements SensorEventListener {

    private FragmentStepCounterBinding binding;
    private SensorManager sensorManager;
    private Sensor stepCounterSensor;

    private float initialSteps = -1;

    // --- VARIABLES POUR LA SIMULATION ---
    private final Handler simulationHandler = new Handler(Looper.getMainLooper());
    private float simulatedSteps = 1250f; // Faux pas accumulés depuis le faux "démarrage"

    private final ActivityResultLauncher<String> permissionLauncher =
            registerForActivityResult(new ActivityResultContracts.RequestPermission(), isGranted -> {
                if (isGranted) {
                    binding.textPermissionWarning.setVisibility(View.GONE);
                    startSensorOrSimulation();
                } else {
                    binding.textPermissionWarning.setText("Permission refusée. Impossible de compter les pas.");
                    binding.textPermissionWarning.setVisibility(View.VISIBLE);
                }
            });

    @Nullable
    @Override
    public View onCreateView(@NonNull LayoutInflater inflater, @Nullable ViewGroup container, @Nullable Bundle savedInstanceState) {
        binding = FragmentStepCounterBinding.inflate(inflater, container, false);

        sensorManager = (SensorManager) requireActivity().getSystemService(Context.SENSOR_SERVICE);
        if (sensorManager != null) {
            stepCounterSensor = sensorManager.getDefaultSensor(Sensor.TYPE_STEP_COUNTER);
        }

        return binding.getRoot();
    }

    @Override
    public void onResume() {
        super.onResume();

        // On demande la permission EN PREMIER (Même si c'est un émulateur, pour voir la fenêtre)
        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.Q &&
                ContextCompat.checkSelfPermission(requireContext(), Manifest.permission.ACTIVITY_RECOGNITION) != PackageManager.PERMISSION_GRANTED) {

            binding.textPermissionWarning.setText("Veuillez accorder la permission d'activité physique.");
            binding.textPermissionWarning.setVisibility(View.VISIBLE);
            permissionLauncher.launch(Manifest.permission.ACTIVITY_RECOGNITION);
        } else {
            binding.textPermissionWarning.setVisibility(View.GONE);
            startSensorOrSimulation();
        }
    }

    private void startSensorOrSimulation() {
        if (stepCounterSensor != null) {
            // Vrai capteur physique présent
            sensorManager.registerListener(this, stepCounterSensor, SensorManager.SENSOR_DELAY_NORMAL);
        } else {
            // Émulateur : Lancement de la simulation
            binding.textPermissionWarning.setText("Capteur physique absent. Simulation activée.");
            binding.textPermissionWarning.setVisibility(View.VISIBLE);
            startSimulation();
        }
    }

    @Override
    public void onPause() {
        super.onPause();
        sensorManager.unregisterListener(this);
        simulationHandler.removeCallbacksAndMessages(null); // Arrêt de la simulation
    }

    @Override
    public void onSensorChanged(SensorEvent event) {
        processSteps(event.values[0]);
    }

    // Méthode commune pour le vrai capteur ET la simulation
    private void processSteps(float totalStepsSinceBoot) {
        if (initialSteps < 0) {
            initialSteps = totalStepsSinceBoot;
        }

        int sessionSteps = (int) (totalStepsSinceBoot - initialSteps);

        binding.textTotalSteps.setText(String.valueOf((int) totalStepsSinceBoot));
        binding.textSessionSteps.setText(String.valueOf(sessionSteps));
    }

    // --- LOGIQUE DE SIMULATION POUR L'ÉMULATEUR ---
    private void startSimulation() {
        simulationHandler.postDelayed(new Runnable() {
            @Override
            public void run() {
                simulatedSteps += 2; // On ajoute 2 pas à chaque seconde
                processSteps(simulatedSteps);
                simulationHandler.postDelayed(this, 1000); // Répète toutes les 1000ms (1 seconde)
            }
        }, 1000);
    }

    @Override
    public void onAccuracyChanged(Sensor sensor, int accuracy) {}

    @Override
    public void onDestroyView() {
        super.onDestroyView();
        binding = null;
    }
}